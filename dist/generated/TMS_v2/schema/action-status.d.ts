/**
 * Action status
 *   * `succeeded`
 *   * `warning`
 *   * `error`
 *   * `fatal`
 *   * `running`
 *   * `initial`
 *   * `unknown`
 *
 */
export type ActionStatus = 'succeeded' | 'warning' | 'error' | 'fatal' | 'running' | 'initial' | 'unknown';
